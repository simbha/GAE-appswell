"""
    A sample non-core config file
    
    Such config file can be used to set config values for third-party services
    (e.g. flickr, twitter) which then can be imported into core.
"""
user="bugs bunny"
password="what's up doc?"
